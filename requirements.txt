# --- Coeur applicatif ---
streamlit==1.41.1
pandas==2.2.3
numpy==2.1.3
plotly==5.24.1

# --- Collecte de donnees ---
# yfinance >= 1.x est requis : les versions 0.2.x n'arrivent plus a negocier
# le cookie/crumb impose par Yahoo Finance et retournent des series vides.
yfinance==1.6.0
curl_cffi==0.16.0
requests==2.32.3

# --- Configuration ---
python-dotenv==1.0.1
